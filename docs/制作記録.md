# 制作記録

## 2026-09-05

- Vite＋素のJavaScriptで、付箋の追加・編集・ドラッグ・削除・ブラウザ保存を課題要件として固定した。
- 重なり防止は、追加時に候補位置と既存付箋の矩形を比較する方式を採用する。
- 新規付箋の配置候補がボード内になくなった場合は、ボードの高さを伸ばして既存付箋の下に配置するようにした。
- `npm install` は共有npmキャッシュの権限エラーで停止したため、既存キャッシュを変更せず、課題専用の一時キャッシュを指定して導入した。
- `npm run build` は成功した。Vite 7.3.6で4モジュールを変換し、`dist/`を生成した。
- `http://127.0.0.1:5174/` はHTTP 200で応答することを確認した。自動ブラウザ連携は環境側モジュールの読み込みエラーで実行できなかったため、幅別の実画面確認は未実施として残した。
- ヘッドレスブラウザでPC幅の初期画面を撮影し、`付箋ボード_確認画面.png`として保存した。撮影後にローカルサーバーを停止し、接続不可であることを確認した。

## 2026-09-09

- AIアプリ開発入門ゼミ 第2回「要件定義」の提出形式に合わせ、`docs/spec.md`と`docs/tasks.md`を追加した。
- 既存の実装範囲をそのまま広げず、MVPを「1画面で付箋を作成・編集・移動・削除・保存する」範囲に固定した。
- 要件定義上の自分の判断として、共同編集・クラウド同期・画像添付を「やらないこと」に明記した。

## 2026-09-15

- EP17〜19の追加機能として、付箋ヘッダーに黄色・水色・ピンク・緑色の色選択ボタンを追加した。
- 色は既存の`color`項目を更新するだけにし、保存データの構造を増やさず、再読み込み後も復元されるようにした。
- 現行版の画面確認で、付箋追加後にも空状態の案内が残る不具合を発見し、`.empty-state[hidden]`の表示を明示して修正した。
- 現行版でビルドと、追加・編集・色変更・ドラッグ・削除・保存復元・3画面幅の自動検証が成功した。実機スマートフォンでの指操作は未確認。
