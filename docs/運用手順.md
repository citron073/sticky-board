# 運用手順

1. `npm install`を実行する。
2. `npm run dev -- --host 127.0.0.1 --port 5174`で起動する。
3. 付箋を追加・編集・移動・削除し、付箋上部の丸い色ボタンで色を変更する。
4. ブラウザを再読み込みし、内容・位置・色が残ることを確認する。
5. EP17〜19の提出時は`EP17-19_提出用レポート.md`、確認画像、プロジェクトフォルダを提出する。
